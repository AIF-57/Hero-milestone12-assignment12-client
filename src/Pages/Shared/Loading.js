import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center h-screen'>
            <button className="btn loading bg-primary border-none text-base-100 rounded-sm">loading . . .</button>
        </div>
    );
};

export default Loading;